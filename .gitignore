/target
*.db
*.db-wal
*.db-shm
.mimir-home/
handoff.md
assets/launch-video.mp4
.playwright-mcp/
.DS_Store

# Personal drafts (blog/marketing) — never shipped in the repo
docs/medium-article.md
docs/*-article.md
# Local-only eval data (labels/ledger at the repo root) — NOT the
# crates/mimir-core/src/eval harness, which ships; the unanchored form
# silently ignored new files there.
/eval/

# Norn arc (tamper-evident eval loop) — local-only, like /eval/
/norn/
# Local agent-harness config (norn write-guard etc.) — never shipped
/.claude/
